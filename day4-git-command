##lets create some git global variables
  git config --global user.name "gopal"
  git config --global user.email "gopal1409@gmail.com"
  git config --global --list

##git init this will intilized the current directory
mkdir projectx
cd projectx/
#lets create some blank file inside the directory 
touch DS_Store
touch auth
touch secret
touch calcy.py
echo "we are going to create a calcy app" > calcy.py
cat calcy.py
#git status will show that this is not an git repo
git status
#to initialized this repo as git repo git init
git init
git status


ls -la
#this will show an .git folder this is the folder where git store your versioing
ls .git
#if you delete teh .git folder you loose all your versioing
rm -rf .git
##########################
git status
####now git is not tracking your file as we have deleted the git folder

#lets do our first commit as we have deleted the git folder we need to reintialzed it
git init
git add calcy.py
git commit -m "started working on calcy application"
#this will show that calcy.py file is not thre
git status
#ls will show the file is there
ls
#git log will show your first commit
git log
#lets modify the calcy.py file and add another line and push to remote
echo "we want to add another function in calcy" >> calcy.py
#git status will compare what code you have previously and what addtion your are doing
git status
cat calcy.py
git add calcy.py
git commit -m "added a new line"
#this will show there another commit
git log
#lets push the changes to remote repo 
git push origin main
#once you push the change to remote repo refresh github you will see 2 line
